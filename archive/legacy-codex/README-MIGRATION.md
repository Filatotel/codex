# Legacy Codex Archive

This directory preserves high-value root/support material from the exact migration source `main@49befc25b36ff7de1a582f95fe5a3a50a6a39fd6`.

It is evidence/history, **not ordinary runtime authority**. In particular, `LOCALFLOW_FACTORY.md`, the old root `AGENTS.md`, `QUICKSTART.md`, global `SKILLS_INDEX.md`, and provider-specific `.codex` hook example must not be used to override current Project Resolver root/engine manifests.

Accepted skills/patterns were moved into active bounded namespaces rather than archived wholesale; Git history plus `ARCHITECTURE_MIGRATION_MAP.md` preserves exact provenance.
